import { Kanit, Anton } from 'next/font/google';
 
export const kanit = Kanit({
    weight: ['400'],
    subsets: ['latin'],
  });

  export const anton = Anton({
    weight: ['400'],
    subsets: ['latin'],
  });

